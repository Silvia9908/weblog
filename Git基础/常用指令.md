



##### 基础配置

```bash
//配置全局
git config --global user.name ""
git config --global user.email 
```

##### 创建仓库

```bash
#在本地项目或在本地文件夹新建一个项目
git init
#这个文件夹就变成了一个git仓库，不需要联网，在这个文件夹里就可以用git功能
#拉取项目
git clone http://
```

##### 修改

```bash
#跟踪一个文件或者目录，暂存指定文件，纳入版本管理
git add <name>
#在git里删除这个文件
git rm <name>
#保留在目录里但不被跟踪
git rm --cache <name>

#文件修改

#修改后，文件状态设置为缓存状态
git add <file-name>
#取消缓存状态
git reset HEAD <name>

#提交此次修改
git commit -m ""
#取消这次提交，不能撤销第一次提交
git reset head~ --soft
```

##### 查看文件状态

```bash
#红色未提交
git status
#查看修改细节
git diff
#查看提交历史 q退出查看
git log

commit fd62a4b5c730728399f2f6b600b8105e7ea45d0c      
Author: miaomiao <1506965472@qq.com>
Date:   Sat Nov 15 19:31:58 2025 +0800
#commit后面是哈希值，每次唯一
#由后面哈希值可以找到每次的提交

#美化输出
git log --pretty=oneline
git log --pretty=format:"自定义格式"
git log --graph#看分支的修改
```

##### 远程仓库/remote

```bash
#将本地仓库关联到远程仓库 首次关联用
git remote add orgin <远程地址>
#orgin只是Git给远程仓库的默认别名
#查看当前关联的远程仓库
git remote -v
#修改远程仓库的名字
git remote rename orgin origin
#修改关联的仓库
git remote set-url origin <新地址>
#本地代码推送远程仓库
#推送本地提交到远程分支
git push origin <分支名>
#拉取远程分支的最新内容到本地
git pull origin <分支>
```

```bash
git commit -am "修复登录逻辑漏洞"
```

> `git commit -am` 是一个常用的组合命令，用于**快速提交工作区中已跟踪文件的修改**，无需手动执行 `git add` 暂存。
>
> - `-a`（`--all`）：自动暂存所有**已跟踪文件**（即之前通过 `git add` 添加过的文件）的修改和删除（但不包括新增的未跟踪文件）。
> - `-m`（`--message`）：直接在命令后添加提交备注（必填）
>
> 1. **不处理新增文件**：如果有新创建的文件（未被 Git 跟踪过），`-a` 不会暂存它们，必须先手动 `git add 新文件` 才能提交。
> 2. **仅针对已跟踪文件**：只对之前 `git add` 过的文件有效，适合快速迭代修改。

##### 分支

每次提交生成新版本的时候会生成提交对象，每一个提交对象都有一个独一无二的哈希值，分支就是包含这个哈希值的文件，理解成指向一个提交对象的指针，可以在一个提交对象上新建多个分支

首次提交对象就是main分支，每次提交时分支也跟着我们提交对象移动，我们在第二次提交对象上新建一个分支，然后在main分支上做一定的修改，提交新的对象，在新建的分支上进行修改再提交

<img src="C:\Users\15069\AppData\Roaming\Typora\typora-user-images\image-20251115204214954.png" alt="image-20251115204214954" style="zoom:50%;" />

<img src="C:\Users\15069\AppData\Roaming\Typora\typora-user-images\image-20251115204348225.png" alt="image-20251115204348225" style="zoom: 80%;" />

###### 分支类型与作用

- **master 分支**：绿色节点，是代码的最终发布版本，带有版本标签（如 Tag 1.0、1.0.1、1.1.0）。
- **develop 分支**：黄色节点，是日常开发的集成分支，用于合并各功能分支的代码。
- **feature 分支**：粉色节点，用于开发新功能，完成后合并回 develop 分支。
- **release 分支**：绿色节点，用于版本发布前的测试与修正，完成后合并回 develop 和 master 分支。
- **hot fixes 分支**：红色节点，用于线上版本的紧急缺陷修复，修复后合并回 develop 和 master 分支。

###### 流程解读

1. 从 master 分支（Tag 1.0）衍生出 develop 分支，开展日常开发。
2. feature 分支从 develop 分支拉出，开发新功能后合并回 develop。
3. 当 develop 分支功能足够时，拉出 release 分支进行发布前的验证与修正，完成后同步修正到 develop 分支，并合并到 master 分支形成新标签（如 Tag 1.1.0）。
4. 若 master 分支的线上版本（如 1.0）出现缺陷，从 master 拉出 hot fixes 分支修复，修复后合并回 develop 和 master 分支，形成新标签（如 Tag 1.0.1）。

```bash
#查看分支
git branch --list
#查看所有分支
git branch
#创建新分支
git branch <新分支名>
#切换到指定分支
git checkout <分支名>
#创建并切换到新分支
git checkout -b <新分支名>
#将指定分支的内容合并到当前分支
git merge <分支名>
#删除本地分支
git branch -d <分支名>
```





##### ssh鉴权

```bash
cd ~/.ssh
ssh-keygen -t rsa -b 4096 -C "评论，最好写邮箱
#-t:密钥的格式 -b:大小 -C:评论
ls#看我们生成的密钥 选公钥
#复制公钥
#添加密钥
```



##### 文件名中文显示\345\237\272\347\241\200

```bash
git config --global core.quotepath false
```



##### **一、仓库初始化与关联**

| 命令                                 | 作用                                                         |
| ------------------------------------ | ------------------------------------------------------------ |
| `git init`                           | 在当前文件夹初始化一个新的 Git 仓库（生成 `.git` 文件夹）    |
| `git clone <仓库地址>`               | 克隆远程仓库到本地（例如 `git clone git@github.com:用户名/仓库名.git`） |
| `git remote add origin <远程地址>`   | 将本地仓库关联到远程仓库（首次关联用）                       |
| `git remote -v`                      | 查看当前关联的远程仓库地址                                   |
| `git remote set-url origin <新地址>` | 修改关联的远程仓库地址（仓库改名后用）                       |

##### **二、文件暂存与提交（本地操作）**

| 命令                       | 作用                                                         |
| -------------------------- | ------------------------------------------------------------ |
| `git status`               | 查看文件修改状态（哪些文件被修改 / 新增 / 删除）             |
| `git add <文件名>`         | 暂存指定文件（例如 `git add index.js`）                      |
| `git add .` / `git add -A` | 暂存所有修改（新增、修改、删除的文件）                       |
| `git commit -m "备注信息"` | 提交暂存区的文件到本地仓库（备注要清晰，例如 “修复登录按钮样式”） |
| `git commit --amend`       | 补充修改最近一次提交（避免多一条无用记录）                   |
| `git reset --hard HEAD~1`  | 撤销最近一次提交并删除修改（谨慎使用，会丢失内容）           |

##### **三、远程同步（推送到 GitHub / 拉取更新）**

| 命令                          | 作用                                                        |
| ----------------------------- | ----------------------------------------------------------- |
| `git pull origin <分支名>`    | 拉取远程分支的最新内容到本地（例如 `git pull origin main`） |
| `git push origin <分支名>`    | 推送本地提交到远程分支（例如 `git push origin main`）       |
| `git push -u origin <分支名>` | 首次推送时绑定本地与远程分支，后续可简化为 `git push`       |
| `git push -f origin <分支名>` | 强制推送（覆盖远程仓库内容，仅自己开发时用）                |

##### **四、分支操作**

| 命令                         | 作用                                                   |
| ---------------------------- | ------------------------------------------------------ |
| `git branch`                 | 查看本地所有分支（当前分支前有 `*`）                   |
| `git branch <新分支名>`      | 创建新分支（例如 `git branch dev`）                    |
| `git checkout <分支名>`      | 切换到指定分支（例如 `git checkout dev`）              |
| `git checkout -b <新分支名>` | 创建并切换到新分支（一步完成）                         |
| `git merge <分支名>`         | 将指定分支的内容合并到当前分支（例如 `git merge dev`） |
| `git branch -d <分支名>`     | 删除本地分支（已合并的分支）                           |

##### **五、版本回溯与查看**

| 命令                       | 作用                                                         |
| -------------------------- | ------------------------------------------------------------ |
| `git log`                  | 查看提交历史（按 `q` 退出查看）                              |
| `git log --oneline`        | 简洁显示提交历史（一行一条）                                 |
| `git checkout <commit-id>` | 回溯到指定版本（`commit-id` 是 `git log` 中显示的哈希值前 6 位即可） |
| `git reflog`               | 查看所有操作记录（包括已删除的提交，用于找回误删版本）       |

##### **六、撤销操作**

| 命令                       | 作用                                         |
| -------------------------- | -------------------------------------------- |
| `git checkout -- <文件名>` | 撤销工作区的修改（恢复到最近一次提交的状态） |
| `git reset HEAD <文件名>`  | 取消暂存（将已 `add` 的文件从暂存区移除）    |

1. 